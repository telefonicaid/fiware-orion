Add:  POST /v2/entities - entity creation under version 2 (Issue #970)
Add:  Version 2 only supports JSON so we can now distinguish between STRINGS, NUMBERS (all treated as floats for now),
      and BOOLEANS (true, false), for the VALUE for ContextAttribute, Metadata and Compounds (Issue #?)
